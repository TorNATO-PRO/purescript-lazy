{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache && rimraf .spago",
    "build": "spago build",
    "test": "spago build && purescm run --main Test.Main"
  },
  "devDependencies": {
    "eslint": "^7.15.0",
    "pulp": "16.0.0-0",
    "purescm": "^1.3.0",
    "purescript": "^0.15.10",
    "purescript-psa": "^0.8.2",
    "rimraf": "^3.0.2",
    "spago": "^0.93.17"
  }
}
